{
  "$schema": "../gen/schemas/desktop-capability.json",
  "identifier": "default",
  "description": "Default permissions for the Freshon app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
